<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:projets, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:projets, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:projet) %></h2>
    <div class="inner">
      <% form_for :projet, url(:projets, :create), :class => :form do |f| %>
        <%= partial "projets/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
