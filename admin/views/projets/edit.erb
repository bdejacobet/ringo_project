<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:projets, :index) %></li>
      <li><%= link_to pat(:new), url(:projets, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:projets, :edit, :id => @projet.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:projet) %></h2>
    <div class="inner">
      <% form_for :projet, url(:projets, :update, :id => @projet.id), :method => :put, :class => :form do |f| %>
        <%= partial "projets/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
