<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:branches, :index) %></li>
      <li><%= link_to pat(:new), url(:branches, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:branches, :edit, :id => @branch.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:branch) %></h2>
    <div class="inner">
      <% form_for :branch, url(:branches, :update, :id => @branch.id), :method => :put, :class => :form do |f| %>
        <%= partial "branches/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
