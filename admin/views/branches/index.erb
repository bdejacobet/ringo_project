<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to pat(:list), url(:branches, :index) %></li>
      <li><%= link_to pat(:new), url(:branches, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:all) %> <% mt(:branches) %></h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first"><%= mat(:branche, :id) %></th>
          <th><%= mat(:branche, :name) %></th>
          <th><%= mat(:branche, :description) %></th>
          <th><%= mat(:branche, :created_at) %></th>
          <th><%= mat(:branche, :updated_at) %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @branches.each do |branche| %>
        <tr>
          <td class="first"><%= branche.id %></td>
          <td><%= branche.name %></td>
          <td><%= branche.description %></td>
          <td><%= branche.created_at %></td>
          <td><%= branche.updated_at %></td>
          <td class="last">
            <%= button_to pat(:edit), url(:branches, :edit, :id => branche.id), :method => :get, :class => :button_to %> |
            <%= button_to pat(:delete), url(:branches, :destroy, :id => branche.id), :method => :delete, :class => :button_to, :onsubmit => "return confirm('#{pat(:confirm)}')" %>
          </td>
        </tr>
        <% end %>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions"></div>
      </div>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
